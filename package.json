{
  "name": "ebsd-analysis-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix web install && python3 -m venv api/.venv && api/.venv/bin/pip install -r api/requirements.txt",
    "dev": "concurrently --kill-others --names API,WEB --prefix-colors cyan,yellow \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "api/.venv/bin/python -m uvicorn app.main:app --app-dir api --reload --port 8000",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "test": "cd api && .venv/bin/python -m pytest"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
